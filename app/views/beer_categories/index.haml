%h1 Beer Categories
%p 
  .list-group
    - @beerscategory.all.each do |category|
      %a.list-group-item
        %h4 #{category.name} 
        %p #{category.description}
        = link_to '',edit_beer_category_path(id: category.id), class: "fa fa-edit"
        / should add an alert, but not as a message box but as an alert box, and for now I will leave it alone. 
        = link_to '', category , class: "fa fa-trash", method: :delete #, data: { confirm: 'Are you sure?' } 
  = link_to "Add Category", new_beer_category_path, class: "btn btn-primary btn-lg"